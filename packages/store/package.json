{
  "name": "@4wc/store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Persistence ports: profiles, settings sync, game history. Adapters are swappable; the app depends only on the ports.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@4wc/engine": "*",
    "@4wc/ui-core": "*"
  }
}
